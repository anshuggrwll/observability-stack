global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "monitoring-app"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - "192.168.1.9:8081" # My laptop's IP address
